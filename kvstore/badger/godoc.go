// Package badger is a wrapper around the BadgerDB key-value store for use in the
// SM(S)T implementation and as a general purpose key-value store for both
// in-memory and persistent use cases.
package badger
